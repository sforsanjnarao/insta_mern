<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Registration</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-black text-white flex justify-center items-center h-screen">
    <div class="w-full max-w-md bg-gray-900 p-6 rounded-lg shadow-lg">
        <h2 class="text-2xl font-bold text-center mb-6">Login</h2>
        
        <form action="/login" method="POST" class="space-y-4">
            

            <!-- Email -->
            <div>
                <label class="block text-gray-300">Email</label>
                <input type="email" name="email" required 
                    class="w-full px-4 py-2 rounded-md bg-gray-800 text-white border border-gray-700 focus:ring-2 focus:ring-gray-600 focus:outline-none">
            </div>

            <!-- Password -->
            <div>
                <label class="block text-gray-300">Password</label>
                <input type="password" name="password" required 
                    class="w-full px-4 py-2 rounded-md bg-gray-800 text-white border border-gray-700 focus:ring-2 focus:ring-gray-600 focus:outline-none">
            </div>

            <!-- Submit Button -->
            <button type="submit"
                class="w-full bg-gray-700 hover:bg-gray-600 transition duration-300 px-4 py-2 rounded-md text-white font-bold">
                login
            </button>
        </form>
    </div>
</body>
</html>